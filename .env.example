# Environment Configuration for ClickView

# Node Environment
NODE_ENV=development

# Backend Server Configuration
PORT=3001

# Database Configuration
DATABASE_URL=postgresql://admin:your_password@localhost:5432/clickup_dashboard

# Redis Configuration (Optional - for caching)
REDIS_URL=redis://localhost:6379

# Security Keys
# IMPORTANT: Generate secure random keys using the provided script:
# Run: npm run generate-keys
# Or manually: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=REPLACE_WITH_64_CHAR_HEX_STRING
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING

# ClickUp Configuration
CLICKUP_WEBHOOK_SECRET=your-webhook-secret

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Logging
LOG_LEVEL=info
LOG_DIR=logs